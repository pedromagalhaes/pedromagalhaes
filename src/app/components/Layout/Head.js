import Head from 'next/head'

import '../../../dmi-ui/stylesheets/app.scss'

export default React.memo(() => (
  <Head>
    <meta charSet='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1, user-scalable=0' />
  </Head>
))
