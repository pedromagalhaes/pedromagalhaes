(function initalizeProxyMiddleware(proxyMiddleware) { // eslint-disable-line no-unused-vars
  const proxy = require('http-proxy-middleware') // eslint-disable-line global-require, no-unused-vars
}(module.exports))
